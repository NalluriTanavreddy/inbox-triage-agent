"""Frontend for the stage 7 UI: a single static index.html served by
the FastAPI backend (src/api/main.py), not a Python package -- this
file exists only so the directory has one, matching the layout of the
other src/ subpackages."""
